using Thinktecture.SmartEnums;

namespace Thinktecture;

/// <summary>
/// Represents a wrapper around the <see cref="ProductType"/> entity.
/// </summary>
public record ProductTypeWrapper(ProductType ProductType);
